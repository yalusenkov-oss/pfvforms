# 🎨 Улучшения UI компонентов

## Что было сделано

### 1. Кастомный компонент DatePicker

Создан стилизованный компонент для выбора дат вместо стандартного `input type="date"`.

**Особенности:**
- ✅ Кастомная иконка календаря в фиолетовом стиле
- ✅ Плавные анимации при наведении
- ✅ Соответствие общему дизайну сайта
- ✅ Поддержка всех браузеров (Chrome, Firefox, Safari)

**Использование:**
```tsx
<DatePicker
  label="Дата релиза"
  required
  icon={<Calendar className="w-4 h-4" />}
  value={data.releaseDate || ''}
  onChange={(e) => onChange('releaseDate', e.target.value)}
  hint="Подсказка"
/>
```

### 2. Кастомный компонент Select

Создан стилизованный компонент для выбора значений вместо стандартного `<select>`.

**Особенности:**
- ✅ Кастомная стрелка вниз (ChevronDown)
- ✅ Плавные анимации при наведении
- ✅ Фиолетовая цветовая схема
- ✅ Адаптивные стили для разных размеров

**Использование:**
```tsx
<Select
  value={artist.type}
  onChange={(value) => handleChange(value)}
  options={[
    { value: 'comma', label: ', (запятая)' },
    { value: 'feat', label: 'feat.' }
  ]}
  className="w-28 text-xs"
/>
```

## Где используются

### DatePicker используется в:
- ✅ `StepOne.tsx` - Дата релиза
- ✅ `StepTwo.tsx` - Дата выдачи паспорта
- ✅ `StepPromo.tsx` - Дата релиза (детальное и еженедельное промо)

### Select используется в:
- ✅ `StepOne.tsx` - Выбор типа артиста (запятая/feat) при добавлении исполнителей

## Стили

Все стили находятся в `src/index.css`:
- `.date-input-custom` - стили для календаря
- `.select-custom` - стили для выпадающего списка

## Визуальные улучшения

1. **Календарь:**
   - Кастомная SVG иконка календаря
   - Фиолетовый цвет при наведении
   - Плавные переходы

2. **Выбор запятых/feat:**
   - Стилизованная стрелка вниз
   - Фиолетовая цветовая схема
   - Адаптивные размеры для маленьких селектов

## Совместимость

- ✅ Chrome/Edge (WebKit)
- ✅ Firefox
- ✅ Safari
- ✅ Мобильные браузеры

Все компоненты полностью совместимы с существующим кодом и не требуют изменений в логике.
