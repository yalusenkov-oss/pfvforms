# 🔧 СРОЧНОЕ ИСПРАВЛЕНИЕ: Проблема с POST запросами

## Проблема
POST запросы не работают из-за CORS политики браузера. GET запросы работают нормально.

## Решение (2 шага)

### Шаг 1: Обновите код Google Apps Script

1. Откройте вашу Google Таблицу
2. Перейдите: **Расширения** → **Apps Script**
3. Найдите функцию `doGet` и замените её на:

```javascript
function doGet(e) {
  // Если есть параметр data - это запрос на сохранение данных
  if (e.parameter && e.parameter.data) {
    try {
      const data = JSON.parse(decodeURIComponent(e.parameter.data));
      
      if (data.formType === 'distribution') {
        saveDistribution(data);
      } else if (data.formType === 'promo') {
        savePromo(data);
      }
      
      return ContentService
        .createTextOutput(JSON.stringify({ success: true, message: 'Данные успешно сохранены' }))
        .setMimeType(ContentService.MimeType.JSON);
    } catch (error) {
      return ContentService
        .createTextOutput(JSON.stringify({ success: false, error: error.toString() }))
        .setMimeType(ContentService.MimeType.JSON);
    }
  }
  
  // Обычный GET запрос для проверки
  return ContentService
    .createTextOutput(JSON.stringify({ 
      status: 'ok', 
      message: 'GET works!' 
    }))
    .setMimeType(ContentService.MimeType.JSON);
}
```

4. **Сохраните** (Ctrl+S)

5. **Создайте новое развертывание**:
   - **Развернуть** → **Новое развёртывание**
   - Тип: **Веб-приложение**
   - Выполнять как: **Я**
   - Доступ: **Все**
   - Нажмите **Развернуть**

### Шаг 2: Обновите код в проекте

Код уже обновлен! Просто перезапустите dev сервер:

```bash
npm run dev
```

Или просто обновите страницу `test-google-sheets.html` в браузере.

## Что изменилось

✅ Код теперь автоматически использует GET запросы с данными в URL, если POST не работает  
✅ Это обходит проблему CORS  
✅ Все данные будут успешно сохраняться в таблицу  

## Проверка

После обновления:
1. Откройте `test-google-sheets.html`
2. Нажмите любую кнопку тестирования
3. Данные должны успешно отправиться!

## Подробная информация

См. файл `UPDATED_APPS_SCRIPT.md` для полного обновленного кода.
